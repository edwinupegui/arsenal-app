---
import Layout from "../../../layouts/Layout.astro";
import CategoryBadge from "../../../components/CategoryBadge.astro";
import {
    getResourceById,
    getCategoryById,
    parseTags,
} from "../../../lib/resources";
import { RESOURCE_TYPE_CONFIG, LANGUAGE_CONFIG } from "../../../lib/constants";

const { id } = Astro.params;
if (!id || isNaN(parseInt(id))) {
    return Astro.redirect("/recursos");
}
const resourceId = parseInt(id);

const resourceResult = getResourceById(resourceId);

if (!resourceResult.ok || !resourceResult.value) {
    return Astro.redirect("/recursos?error=not-found");
}

const resource = resourceResult.value;

if (resource.deletedAt) {
    return Astro.redirect("/recursos/trash");
}

const categoryResult = getCategoryById(resource.categoryId);
const tags = parseTags(resource.tags);

const languageConfig =
    LANGUAGE_CONFIG[resource.language as keyof typeof LANGUAGE_CONFIG] ??
    LANGUAGE_CONFIG.EN;
const typeConfig =
    RESOURCE_TYPE_CONFIG[resource.type as keyof typeof RESOURCE_TYPE_CONFIG] ??
    RESOURCE_TYPE_CONFIG.video;

// Type colors for visual distinction
const typeColors = {
    video:   { border: '0.62 0.22 25',   glow: '0.62 0.22 25'   },
    article: { border: '0.65 0.18 200',  glow: '0.65 0.18 200'  },
    tool:    { border: '0.68 0.18 155',  glow: '0.68 0.18 155'  },
    repo:    { border: '0.65 0.18 290',  glow: '0.65 0.18 290'  },
};

const typeStyle = typeColors[resource.type as keyof typeof typeColors] || typeColors.video;
---

<Layout
    title={`${resource.title} — Arsenal`}
    description={resource.description || `Recurso: ${resource.title}`}
>
    <div class="max-w-3xl mx-auto">
        <!-- Breadcrumb navigation -->
        <nav class="mb-8 flex items-center gap-2 text-sm animate-fade-left">
            <a
                href="/recursos"
                class="text-text-muted hover:text-text transition-colors duration-150 flex items-center gap-1.5 group"
            >
                <svg class="w-4 h-4 transition-transform group-hover:-translate-x-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                </svg>
                Recursos
            </a>
            <span class="text-text-subtle">/</span>
            <span class="text-text truncate max-w-50">{resource.title}</span>
        </nav>

        <!-- Main content card -->
        <article
            class="bg-surface border border-border rounded-2xl overflow-hidden animate-fade-up"
            style={`--type-border: oklch(${typeStyle.border}); --type-glow: oklch(${typeStyle.glow});`}
        >
            <!-- Header with accent line -->
            <div class="relative">
                <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-current to-transparent opacity-30" style={`color: var(--type-border);`}></div>
                <div class="p-6 pb-5">
                    <div class="flex items-start gap-5">
                        <!-- Type icon -->
                        <div
                            class="shrink-0 w-14 h-14 rounded-xl flex items-center justify-center border"
                            style={`background-color: oklch(${typeStyle.border} / 0.12); border-color: oklch(${typeStyle.border} / 0.3); color: oklch(${typeStyle.border});`}
                        >
                            <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d={typeConfig.icon}></path>
                            </svg>
                        </div>

                        <div class="flex-1 min-w-0">
                            <h1 class="text-2xl font-bold text-text tracking-tight leading-tight">
                                {resource.title}
                            </h1>
                            <a
                                href={resource.url}
                                target="_blank"
                                rel="noopener noreferrer"
                                class="text-sm text-text-muted hover:text-accent transition-colors duration-150 flex items-center gap-2 mt-2 group/link"
                            >
                                <svg class="w-4 h-4 opacity-60 group-hover/link:opacity-100 group-hover/link:text-accent transition-all" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
                                </svg>
                                <span class="truncate">{resource.url}</span>
                            </a>
                        </div>

                        <!-- Open link button -->
                        <a
                            href={resource.url}
                            target="_blank"
                            rel="noopener noreferrer"
                            class="px-5 py-2.5 bg-accent hover:bg-accent-hover text-white text-sm font-medium rounded-xl transition-all duration-150 active:scale-[0.97] flex items-center gap-2 shadow-sm hover:shadow-glow-sm shrink-0"
                        >
                            Abrir
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
                            </svg>
                        </a>
                    </div>
                </div>
            </div>

            <!-- Description -->
            {
                resource.description && (
                    <div class="px-6 py-5 border-t border-border">
                        <p class="text-text leading-relaxed">
                            {resource.description}
                        </p>
                    </div>
                )
            }

            <!-- Metadata section -->
            <div class="px-6 py-5 bg-overlay/50 border-t border-border">
                <div class="flex flex-wrap items-center gap-3">
                    <!-- Language badge -->
                    <span
                        class="inline-flex items-center gap-1.5 px-2.5 py-1 text-xs font-medium rounded border"
                        style={`background-color: oklch(${languageConfig.color} / 0.12); border-color: oklch(${languageConfig.color} / 0.3); color: oklch(${languageConfig.color});`}
                    >
                        <svg class="w-3 h-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            {resource.language === 'ES' ? (
                                <path d="M21 3H3v7l3 3 3-3 3 3 3-3 3 3V3zM3 14h18M3 18h18" />
                            ) : (
                                <path d="M12 3v18M3 9h18M3 15h18" />
                            )}
                        </svg>
                        {languageConfig.label}
                    </span>

                    <!-- Type badge -->
                    <span
                        class="inline-flex items-center gap-1.5 px-2.5 py-1 text-xs rounded border"
                        style={`background-color: oklch(${typeStyle.border} / 0.1); border-color: oklch(${typeStyle.border} / 0.3); color: oklch(${typeStyle.border});`}
                    >
                        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d={typeConfig.icon}></path>
                        </svg>
                        {typeConfig.label}
                    </span>

                    <!-- Category badge -->
                    {
                        categoryResult.ok && categoryResult.value && (
                            <CategoryBadge
                                name={categoryResult.value.name}
                                icon={categoryResult.value.icon}
                                size="md"
                            />
                        )
                    }

                    <!-- Tags -->
                    {
                        tags.length > 0 && (
                            <div class="flex flex-wrap gap-2 ml-auto">
                                {tags.map((tag: string) => (
                                    <span
                                        class="px-2 py-0.5 text-xs rounded border text-text-subtle hover:text-text-muted transition-colors"
                                        style="background-color: oklch(0.40 0.02 280 / 0.08); border-color: oklch(0.40 0.02 280 / 0.15);"
                                    >
                                        #{tag}
                                    </span>
                                ))}
                            </div>
                        )
                    }
                </div>
            </div>

            <!-- Actions -->
            <div
                class="px-6 py-4 border-t border-border flex items-center justify-between bg-surface"
            >
                        <a
                            href={`/recursos/${resource.id}/edit`}
                            class="px-5 py-2.5 bg-accent hover:bg-accent-hover text-white text-sm font-medium rounded-lg transition-colors duration-150 active:scale-[0.97] flex items-center gap-2 shrink-0"
                        >
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                    </svg>
                    Editar
                </a>

                <form
                    method="POST"
                    action={`/api/resources/${resource.id}`}
                    class="flex items-center gap-3"
                >
                    <input type="hidden" name="_method" value="DELETE" />
                    <button
                        type="submit"
                        class="px-4 py-2 text-red-400/70 hover:text-red-400 text-sm font-medium transition-colors duration-150 flex items-center gap-2 hover:bg-red-400/10 rounded-lg px-3 py-2"
                    >
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                        </svg>
                        Enviar a papelera
                    </button>
                </form>
            </div>
        </article>
    </div>
</Layout>